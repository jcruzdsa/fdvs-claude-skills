# Cortex Phase 2 Goal Template

When filling this in, replace all `{{PLACEHOLDER}}` tokens with real values from Phase 1.

---

## Filled Goal String (copy everything between the START and END markers)

<!-- START GOAL STRING -->
You are running a SQL quality check battery against a Snowflake table (role: FR_ANALYST, warehouse: VWH_DSA_RPT_PRD).

TARGET TABLE: {{TABLE_FQTN}}
JIRA TICKET: {{TICKET_KEY}}

TEST PLAN:
{{TEST_PLAN}}

INSTRUCTIONS:
1. For each check in the test plan, write and execute a SQL query against {{TABLE_FQTN}}.
2. Record: check number, check description, SQL used, result (PASS or FAIL), and any failing example rows (up to 5).
3. A check PASSES if it meets its stated condition. A check FAILS otherwise.
4. After running all checks, write an HTML report to: {{REPORT_PATH}}
   (REPORT_PATH = ~/Documents/uat-reports/{{TICKET_KEY}}_{{TABLE}}_{{REPORT_DATE}}.html)

HTML REPORT FORMAT:
- Header: table name, ticket key, run timestamp
- Summary row: total checks, passed, failed
- One row per check: check #, description, source ([FROM TICKET] or [INFERRED]), SQL, result (green PASS / red FAIL), failing examples if any
- Footer: "Generated by uat-validator via Cortex Code CLI"

Use role FR_ANALYST. If VWH_DSA_RPT_PRD is unavailable, fall back to VWH_DSA_RPT_SM.
Write only to: ~/Documents/uat-reports/
Do not write to any Snowflake schema.
<!-- END GOAL STRING -->
